# Verify Skills

1. Ensure every skill is in `skills/<name>/SKILL.md`.
2. Validate frontmatter (`name`, `description`).
3. Confirm progressive disclosure links resolve.

Use:

- `../implement-agent-skills/scripts/validate-skill-structure.sh`
- `../implement-agent-skills/scripts/check-skill-frontmatter.sh`
