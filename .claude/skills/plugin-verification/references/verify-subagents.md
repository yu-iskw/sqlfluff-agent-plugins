# Verify Sub-Agents

1. Validate each agent file is markdown with required sections.
2. Check frontmatter fields.
3. Confirm delegation and permissions are documented.

Use:

- `../implement-sub-agents/scripts/validate-subagent.sh`
- `../implement-sub-agents/scripts/check-subagent-frontmatter.sh`
