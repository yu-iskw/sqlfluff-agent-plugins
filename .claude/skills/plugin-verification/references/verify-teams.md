# Verify Agent Teams

1. Validate team config JSON.
2. Verify environment supports required CLI/runtime behavior.
3. Check coordination documentation and display mode choices.

Use:

- `../implement-agent-teams/scripts/check-team-config.sh`
- `../implement-agent-teams/scripts/verify-team-env.sh`
