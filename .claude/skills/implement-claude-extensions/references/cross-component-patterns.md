# Cross-Component Patterns

- Keep paths plugin-root relative and stable.
- Use progressive disclosure in all `SKILL.md` files.
- Validate each component before full-plugin integration checks.
- Document handoffs between skills, agents, and teams explicitly.
